# AGENTS.md

## 역할

이 저장소는 "AI 시대의 인지적 퇴행 방지를 위한 학술 논문 독해 헬퍼" 프로토타입을 만드는 프로젝트다.

이 저장소에서 작업하는 에이전트는 시니어 풀스택 엔지니어이자 UX 프로토타입 디자이너로 행동한다. 목표는 일반적인 AI 논문 요약기가 아니라, 사용자가 원문을 직접 읽고, AI 결과를 검증하고, 지식을 능동적으로 구조화하도록 만드는 "생산적 저항 기반 논문 독해 도구"를 구현하는 것이다.

## 제품 철학

- AI가 사용자의 사고를 대체하지 않는다.
- AI는 원문에서 지식 파편, 근거 문장, 검증 과업, 구조화 힌트를 제공한다.
- 사용자는 드래그, 연결, 오류 탐지, 원문 대조를 통해 직접 인지적 노력을 수행해야 한다.
- 최종 요약은 사용자가 최소한의 검증과 구조화 과업을 완료한 뒤에만 공개된다.
- 거짓 정보는 학습용 "인공적 노이즈"로만 사용하며, 절대 정답 요약이나 저장된 지식으로 남기지 않는다.

## 기술 스택 기본값

- Next.js App Router
- TypeScript
- React
- Tailwind CSS
- shadcn/ui 또는 최소한의 자체 UI 컴포넌트
- React Flow 또는 동등한 라이브러리로 지식 구조화 캔버스 구현
- PDF.js 계열 라이브러리로 PDF 렌더링 및 텍스트 위치 매핑
- OpenAI JavaScript SDK
- Zod로 API 입력/출력 스키마 정의
- Vitest로 핵심 로직 테스트
- Playwright는 가능하면 e2e 테스트에 사용

## OpenAI API 원칙

- OpenAI 호출은 서버 라우트에서만 수행한다. 클라이언트에 API 키를 노출하지 않는다.
- `OPENAI_API_KEY`는 `.env.local`에만 둔다.
- `OPENAI_MODEL` 환경변수로 모델을 바꿀 수 있게 한다.
- Responses API를 사용한다.
- 모든 모델 출력은 가능한 한 Structured Outputs 또는 Zod 스키마 기반으로 강제한다.
- 모델이 자유 텍스트로 반환한 값을 임의 파싱하지 않는다.
- 모든 AI 생성 문장은 반드시 원문 `evidenceId`를 가져야 한다.
- 근거가 부족하면 `confidence`를 낮추고 `needsUserVerification: true`로 표시한다.

## UX 원칙

- UI 언어는 한국어를 기본으로 한다.
- 왼쪽은 PDF 원문 뷰어, 오른쪽은 지식 구조화 캔버스와 검증 패널이다.
- 사용자가 AI 요약만 읽고 끝낼 수 없도록 설계한다.
- 단, 불편함이 과도해지지 않도록 진행률, 피드백, 정답 공개, 힌트를 제공한다.
- 접근성을 고려한다. 드래그 앤 드롭 기능에는 키보드 대체 동작도 제공한다.

## 구현 시 금지 사항

- `NEXT_PUBLIC_OPENAI_API_KEY` 사용 금지.
- AI가 생성한 오류 문장을 실제 요약으로 저장 금지.
- 원문 근거 없는 요약 문장 생성 금지.
- 사용자가 검증 게이트웨이를 통과하기 전 전체 요약 공개 금지.
- PDF 원문 전체를 매번 API에 보내는 비효율적 구현 금지. 섹션 단위 chunking과 캐싱을 사용한다.
- 테스트 없는 핵심 로직 구현 금지.

## 완료 기준

- `pnpm dev`로 실행된다.
- PDF 업로드 후 원문 뷰어가 표시된다.
- 지식 파편이 생성되고, 사용자가 캔버스로 드래그해 구조화할 수 있다.
- 챕터 요약 공개 전 검증 게이트웨이가 뜬다.
- 검증 게이트웨이는 3개의 사실 문장과 1개의 의도적 오류 문장을 포함한다.
- 사용자가 오류를 찾아야 정답 요약이 공개된다.
- 요약 문장 hover 또는 click 시 원문 근거가 하이라이트된다.
- 인지 참여도 대시보드가 상호작용 빈도, 오류 탐지 정확도, 구조화 진행률, 원문 대조율을 기반으로 갱신된다.
- `pnpm lint`, `pnpm test`, `pnpm build`가 통과한다.
- README에 설치법, 환경변수, 데모 플로우, 한계점, 향후 개선점이 정리되어 있다.

## 작업 방식

- 기존 코드와 디자인 패턴을 먼저 읽고, 그 흐름에 맞춰 작게 구현한다.
- 핵심 도메인 로직은 Zod 스키마와 테스트를 함께 추가한다.
- AI 결과는 항상 원문 근거, 신뢰도, 사용자 검증 필요 여부를 포함하는 구조로 다룬다.
- 사용자가 검증하기 전에는 전체 요약을 노출하지 않는 상태 모델을 유지한다.
- 의도적 오류 문장은 검증 과업 안에서만 존재해야 하며, 저장되는 지식이나 최종 요약 데이터와 분리한다.
- PDF 처리에는 섹션 단위 chunking, evidence id, 캐싱 전략을 기본으로 둔다.
- 접근성, 키보드 조작, 상태 피드백을 프로토타입 단계에서도 빼지 않는다.
